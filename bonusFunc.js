// As further practice with JS Arrays, loops and functions, rebuild the following methods using only a for loop.
//
// map()
// reduce()
// filter()

Array.myMap = function(callBack, i, a){
	var newArray = [];
	for(var i = 0; i < arg; i++){
		newArray.push(arg, i, thisN)
	};

	return newArray;
};

Array.myReduce = function(inital, next){
	var newArray = [];
	for(var i = 0; i , i++){
		
	}

}